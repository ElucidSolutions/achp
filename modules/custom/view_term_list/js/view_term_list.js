/*
  This module detects clicks on View term
  list filters and interfaces with the Drupal
  Select Term command to apply the selected
  filter terms.
*/
(function ($, Drupal) {
  /*
  */
  Drupal.AjaxCommands.prototype.selectTerm = function (ajax, response, status) {
  }
}) (jQuery, Drupal);
