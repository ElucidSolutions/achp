{#
/**
 * @file
 * Default theme implementation to display a formatted image field.
 *
 * Available variables:
 * - image: A collection of image data.
 * - image_style: An optional image style.
 * - url: An optional URL the image can be linked to.
 * - image_alt: The image alt value.
 * - image_title: The image title value.
 * - field_name: The name of the field that this image belongs to.
 *
 * @see template_preprocess_lightbox_image_formatter()
 *
 * @ingroup themeable
 */
#}
{% if url %}
  <a data-lightbox="{{ field_name }}" data-title="{{ image_alt }}" href="{{ url }}">{{ image }}</a>
{% else %}
  {{ image }}
{% endif %}
